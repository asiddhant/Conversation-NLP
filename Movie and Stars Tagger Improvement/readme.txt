1) Both movie and stars extractor work similarly
2) dataset.csv contains the data in both folders
3) Three files have been used for feature extraction
    stanfordgr.py
    nertagger.py
    featureextractor.R
4) trainingntesting.R has codes for training and testing.
5) 300features_40minwords_10context is the trained word2vec
   model from which features have been used.

6) Other files are copies of these files.
7) workspace.RData has the saved session